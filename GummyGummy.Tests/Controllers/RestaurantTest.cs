using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using GummyGummy.Models;
using GummyGummy.Controllers;

namespace GummyGummy.Tests.Controllers
{
    [TestClass]
    public class RestaurantTest
    {
        [TestMethod]
        public void Create_Restaurant_when_Model_Valid()
        {
            //var db = new FakeRestaurantDb();
            //var sqldb = new RestaurantDB();
            //var controller = new RestaurantsController(sqldb);
            //var view = controller.Create(new Restaurant() { Name = "Chipotle" });
            //Assert.AreEqual(1, sqldb.SaveChanges());
        }
    }
}
